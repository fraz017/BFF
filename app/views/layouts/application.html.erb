<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>3wrongs</title>
    <!-- Tell the browser to be responsive to screen width -->
    <%= favicon_link_tag "3wrongs-flower-32x32.png" %>
        <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
        <%= csrf_meta_tags %>
            <script src="//maps.googleapis.com/maps/api/js?sensor=false&amp;libraries=places&key=AIzaSyB5ngnBJFz1SBjnhwmEbdR6egLpFTvbcrA"></script>
            <%= stylesheet_link_tag    'application', media: 'all' %>
                <link rel="stylesheet" href="http://code.jquery.com/ui/1.8.21/themes/base/jquery-ui.css" />
                <link rel='stylesheet prefetch' href='https://fonts.googleapis.com/css?family=Roboto:400,700,300'>
                <link rel='stylesheet prefetch' href='https://cdnjs.cloudflare.com/ajax/libs/material-design-iconic-font/2.1.2/css/material-design-iconic-font.min.css'>
                <link rel='stylesheet prefetch' href='https://rawgit.com/marvelapp/devices.css/master/assets/devices.min.css'>
                <%= javascript_include_tag 'application' %>
                    <%= javascript_include_tag 'cable' %>
                        <script src='https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.13.0/moment.min.js'></script>
</head>

<body>
    <div class="page">
        <% unless flash.empty? %>
            <script type="text/javascript">
                <% flash.each do |f| %>
                <% type = f[0].to_s.gsub('alert', 'error').gsub('notice', 'info') %>
                toastr['<%= type %>']('<%= f[1] %>');
                <% end %>
            </script>
            <% end %>
                <div class="">
                    <div class="screen">
                        <div class="screen-container">
                            <div class="status-bar hidden">
                                <div class="time hidden"></div>
                            </div>
                            <div class="chat">
                                <div class="chat-container">
                                    <%= render partial: "layouts/header" %>
                                        <%= yield %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
    </div>
    <div class="modal fade" id="startChat" data-backdrop="static" tabindex="-1" role="dialog">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h3 class="modal-title">Chat</h3>
                </div>
                <div class="modal-body">
                    <p><strong>3wrongs wants to chat...</strong></p>
                    <div class="row">
                        <div class="col-xs-6">
                            <a href="#" id="accept" class="btn btn-primary pull-right">Accept</a>
                        </div>
                        <div class="col-xs-6">
                            <a href="#" id="reject" class="btn btn-danger">Reject</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>